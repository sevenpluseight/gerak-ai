# GerakAI

> **Note**: Currently, deploying the page using GitHub Pages may appear as **blank**. Fixes have been made, but the issue persists.

## Running Locally
```bash
npm install
git checkout gh-pages
npm run dev
```

- The `gh-pages` branch contains the latest changes of the code
